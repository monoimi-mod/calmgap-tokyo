{
  "name": "calmgap-tokyo-web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "カームダウン・クールダウンスペース 設置優先度マップ",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "deploy": "npm run build && wrangler deploy",
    "deploy:dry": "npm run build && wrangler deploy --dry-run"
  },
  "dependencies": {
    "maplibre-gl": "^5.9.0"
  },
  "devDependencies": {
    "typescript": "^5.9.0",
    "vite": "^7.1.0"
  }
}
