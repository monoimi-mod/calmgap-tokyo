# calmgap-tokyo — 凍結検証レポート（提出前）

| | |
|---|---|
| 監査日 | **2026-08-03** |
| 前回監査 | 2026-07-31（同じファイルを全面改訂。帰趨は 4 章） |
| 対象 | `/Users/hiroaki/Dev/calmgap-tokyo`（ブランチ `claude/continue-session-vsfetr`） |
| 最終コミット | `501caed 現況まとめを 08-02 時点に更新し、直近 3 日の変更を要約する` |
| 作業ツリー | クリーン（未コミットなし） |
| **凍結対象ビルド** | `generated_at` = **2026-08-02T06:00:47+00:00**（`web/public/data/meta.json`） |
| 公開先 | https://calmgap-tokyo.hiroaki-kuwabara.workers.dev （監査時は **2026-07-31 のビルドのまま**。2 章 B-1 → **同日に反映済み。10 章**） |
| 監査者 | Claude Opus 5（Claude Code） |

---

## 0. この監査の目的と合格条件

前回（07-31）の監査は「設計に穴はないか」を初見の人向けに洗うものだった。
その指摘の多くは既に閉じている（4 章）。**今回の目的は違う。**

提出直前のいま必要なのは、改善案ではなく**凍結検証**である。合格条件を 3 つに置いた。

1. **コード・配信データ・文書・公開サイトが 1 つのビルドで整合している**こと
2. **配信データから独立に検算した値が、文書の記載と一致する**こと
3. **質疑で崩れる箇所が無い**こと——取り下げた数字が残っていないか、
   不利な数字が正しく不利なまま書かれているか

改善案は「提出後の宿題」（8 章）へ分離した。いまモデルを触ると提言が全部
入れ替わるためで、これは `docs/issues.md` A4 で既に決めてある方針に従っている。

**結論を先に書く。**

| 合格条件 | 監査時点 | **是正後**（10 章） |
|---|---|---|
| 1. 1 つのビルドで整合 | ❌ **公開サイトだけ 2 日古い**（B-1） | ✅ **解消**——`npm run deploy` 実行、公開 `meta.json` が手元と完全一致 |
| 2. 独立検算が文書と一致 | ❌ **12 項目が不一致**（3 章）。うち 1 件は検査スクリプトが誤りを固定している（B-2） | ✅ **全 18 項目が一致**（検査を 10 → 18 項目へ拡張） |
| 3. 質疑で崩れない | ⚠️ **「渋谷駅前は 3,469 位」は崩れる**（B-2）。他は不利な数字も含めて正しく書かれている | ✅ **792 位へ是正**。誤ったメッシュを見ていた検査も直した |

**ただし配信データそのものは健全である。** 再ビルドしてバイト単位で一致し
（1.3 節）、`meta.json` の主要指標は独立検算とすべて一致した（1.2 節）。
**不一致は「文書側」と「公開側」に閉じている。**
つまり**このビルドは凍結してよく、直すのは文書と公開だけで足りる。**

---

## 1. 実際に走らせたもの

### 1.1 検証コマンド

| コマンド | 結果 |
|---|---|
| `python -m etl.selftest` | ✅ **63 件すべて通過** |
| `node tools/parity_check.mjs` | ✅ 全 **9,507** メッシュで最大乖離 **0.000e+0** |
| `npx tsc --noEmit`（`web/`） | ✅ 型エラー 0 |
| `python tools/doc_numbers.py` | ✅ 10 項目すべて一致（**ただし 1 項目が誤ったメッシュを見ている。B-2**） |
| `python -m etl.build --live --report` | ✅ 正常終了 |
| 公開 URL の `meta.json` | ❌ **`generated_at` が 2026-07-31T02:31:13Z**（手元は 08-02T06:00:47Z） |

### 1.2 配信データからの独立検算

`etl` の関数を通さず、`web/public/data/*.json` を直接集計して照合した
（手順は付録 A）。

| 項目 | 独立検算 | `meta.json` / 文書 | 判定 |
|---|---|---|---|
| メッシュ数 | 9,507 | 9,507 | 一致 |
| 到達不可 | **1,058 件（11.13%）** | 1,058（11.1%） | 一致 |
| うち区内で優先度が中位以上 | **151 件** | 151 | 一致（区ごとの中央値を再計算して確認） |
| 上位 10 の区内訳 | **文京 4 / 豊島 3 / 千代田 2 / 江東 1** | 同 | 一致 |
| 上位 20 の区内訳 | **豊島 6 / 千代田 6 / 文京 4 / 板橋 2 / 江東 1 / 新宿 1** | 同 | 一致 |
| 上位 50 のうち到達不可 | **0 件** | （前回監査は 2 件） | ビルドで変化。現行文書に記載なし |
| 提言 | **11 件**（顔ぶれ・区画数とも一致） | 11 件 | 一致 |
| ホスト内訳 | 区民センター 530 / 児童館 501 / 図書館 247 / 出張所 160 / 文化施設 76 / 公園管理施設 27 = **1,541** | 1,541 | 一致 |
| 構成要素間の最大相関 | **0.804**（駅 × 精神科） | 0.804 | 一致 |
| VIF 最大 | **3.36**（精神科） | 3.36 | 一致 |
| 第 1 主成分の寄与率 | **34.5%** | 34.5% | 一致 |
| 騒音の観測圏外 | **1,095 セル** | 1,095 | 一致 |
| 特別支援学校の実在籍者数 | **42 / 45 校・計 9,926 人** | 同 | 一致 |
| 同 規模の範囲 | **22〜654 人（29.7 倍）** | 「29〜654 人と 22 倍」 | ❌ **私立の愛育学園 22 人を落として都立の最小を最小としていた**（3 章 D-6） |
| 区別の到達不可率 | 江東 **38.7%** / 大田 **38.2%** | 江東 38.8% / 大田 38.1% | ❌ **入れ替わっている**（D-5） |
| 定員が実測の事業所 | **4,370 / 14,118 = 31.0%** | 28.7% | ❌ **2 区時点の値**（D-7） |
| ホスト 100m 以内の組 | **459 組**（別種別 381） | 595 組（別種別 440） | ❌ **再現できない**（D-8） |

### 1.3 再ビルドの再現性 — 凍結の前提として最も重要

`data/processed/` から `python -m etl.build --live --report` を実行し、
配信データと **バイト単位で比較**した。

| ファイル | 結果 |
|---|---|
| `mesh.geojson`（6,679,930 B） | **完全一致** |
| `proposals.json` | **完全一致** |
| `cards.json` | **完全一致** |
| `hosts.geojson` | **完全一致** |
| `demand_points.geojson` | **完全一致** |
| `meta.json` | `generated_at` のみ差分（同じ 10,967 B） |

**同一環境なら、凍結したビルドは何度でも作り直せる。**
`docs/issues.md` B4（GEOS のバージョン差で 4 桁目がズレる）は**別環境での**話で、
この環境での再現性は確認できた。プレゼン中に手元で再ビルドしても数値は動かない。

（検証後、`git checkout -- web/public/data/` で凍結時のバイト列へ戻してある。）

---

## 2. 凍結を妨げる所見

### B-1. 公開サイトが 2 日前のビルドのまま —— 主張の起点が公開画面に無い

| | 公開サイト | 手元（凍結対象） |
|---|---|---|
| `generated_at` | **2026-07-31T02:31:13Z** | 2026-08-02T06:00:47Z |
| `meta.unreachable` | **無い**（`null`） | `{count: 1058, ratio: 0.1113, mid_or_above: 151}` |
| `meta.proposal_top_n` / `proposal_limit` | **無い** | 40 / 12 |
| 提言 | **6 件** | **11 件** |
| 提言の顔ぶれ | 豊島区 池袋・北池袋 / 豊島区 大塚 / 豊島区 池袋・東池袋 / 千代田区ほか 神保町・水道橋 / 文京区 御茶ノ水 / 新宿区 都庁前 | 豊島区 大塚・北池袋 / 文京区ほか 水道橋・飯田橋 / 江東区 亀戸 / 板橋区 板橋・新板橋 / 豊島区 東池袋・都電雑司ヶ谷 / 千代田区ほか 岩本町・秋葉原 / 墨田区 錦糸町 / 豊島区 巣鴨 / 文京区 本郷三丁目 / 中央区 新日本橋 / 新宿区 新宿三丁目 |
| 特別支援学校の帯域 | 1,200m | 800m |

深刻度 **Critical**。理由は件数の差ではない。

**`docs/status.md` は「主張の起点は到達不可にする」と決め、`web/src/ui.ts` は
見出しを到達不可へ差し替えた（コミット `0c5ad1f`）。その `unreachable` が
公開版の `meta.json` に無い。** つまり**この作品でいちばん頑健だと結論した出力が、
公開画面には載っていない。** 審査員が URL を開くと、07-31 の別の結論
（提言 6 件・神保町・都庁前）を見ることになる。

`docs/handoff-hosts.md:9` は公開先を「**未反映**」と書いている。前回監査では
この記載が古く（当時は反映済み）誤りだと指摘したが、**現在は結果的に正しい。**
ただし「何が未反映か」は書かれていない。

**対処**: `web/` で `npm run deploy`。ビルドは再現できる（1.3）ので、
配信データを作り直す必要はない。

### B-2. 「渋谷駅前」として検査・引用しているメッシュが、渋谷駅前ではない

`tools/doc_numbers.py:74` は渋谷駅前のメッシュコードを **`5339358611`** と
決め打ちしている。このメッシュを配信データで引くと:

| | `5339358611`（検査が見ている） | `5339358633`（渋谷駅を含む） |
|---|---|---|
| 範囲 | 35.6500〜35.6521 N | **35.6563〜35.6583 N** |
| 渋谷駅（35.6580, 139.7016）を含むか | **含まない**（約 700m 南） | **含む** |
| `f_station_name` | **代官山** | **渋谷** |
| `f_station_riders` | 28,772 人/日 | **2,897,703 人/日** |
| `f_zoning_name` | **第二種低層住居専用地域** | **商業地域** |
| 需要 / 負荷 | 0.767 / 0.415 | 0.726 / 0.995 |
| 優先度（順位） | 0.629（**3,469 位**） | **0.915（792 位）** |

**渋谷駅前の現在の順位は 3,469 位ではなく 792 位 / 9,507 件（上位 8.3%）である。**

深刻度 **High**。三つの理由から。

1. **検査が誤りを固定している。** このメッシュコードはコミット `0c5ad1f`——
   「過去の数字を引用しない」を機械的に守らせるために `doc_numbers.py` を
   足したまさにそのコミット——で入った。以後 `docs/status.md`・`CLAUDE.md`・
   `docs/methodology.md` の本文が検査の出力（3,469 位）に合わせて書き換えられ、
   **CI は今後もずっと緑になる。**
2. **主張の強さが変わる。** 「上位は渋谷駅前ではない」は 792 位でも言える
   （提言は上位 40 区画から作るので入らない）。だが **3,469 位＝中位という
   説明は成り立たない。** 上位 8.3% は「相対的には高い。ただし最上位ではない」である。
3. **その場で確かめられてしまう。** 公開画面で渋谷駅をクリックすれば
   優先度 0.915 と出る。掴みに使う数字ほど検証されやすい。

**文書内でも矛盾している。** `docs/methodology.md` の比較表（397 行）は
**正しいメッシュ `5339358633`** を使っているが、値は 1,200m 時点のもの（642 位）。
その 2 行上（395 行）の本文だけが 3,469 位に差し替わっている。
**前回監査（07-31）の再現手順（付録 A）も `5339358633` を使っており、
当時の 519 位はこのメッシュの値だった。** 途中でコードだけがすり替わっている。

**対処**: `tools/doc_numbers.py:74` を `5339358633` に直し、
`docs/status.md:21,360`・`CLAUDE.md:300`・`docs/methodology.md:395,404,1210` を
**792 位 / 優先度 0.915** で書き直す。`methodology.md` の比較表は
現在値（下表）へ差し替える。

| | 渋谷駅前 `5339358633` | 上位 1 位 `5339457833`（豊島区 大塚） |
|---|---|---|
| 駅乗降規模 | 1.00 | 0.76 |
| 用途地域 | 1.00 | 0.89 |
| 障害福祉サービス事業所 | **0.35** | **0.96** |
| 特別支援学校 | **0.00** | **0.69** |
| 需要 / 負荷 | 0.726 / 0.995 | 0.995 / 0.995 |
| 優先度（順位） | 0.915（**792 位**） | 1.000（1 位） |

**「負荷は最大でも需要側が中位」という説明そのものは、値を差し替えても成り立つ**
（事業所 0.35 対 0.96、特別支援学校 0.00 対 0.69）。むしろ帯域 800m で
特別支援学校が **0.00** になったことが効いており、A4 の依存を説明する例としては
以前より鮮明である。

---

## 3. 文書と配信データの不一致

**性格が 1 つに揃っている。** 12 件すべてが「**測り直したあと、一部の文書だけ
更新した**」形である。とくに **2026-08-02 の帯域 800m 化で感度分析を全部
測り直したのに、`issues.md` A4 の比較表と `status.md` の一部しか追随していない。**

`tools/doc_numbers.py` が見るのは `meta.json` 由来の 10 個のスカラーだけで、
**`sensitivity.json` の値は 1 つも検査されていない。** 変化がいちばん大きく、
かつ「不利な結果」として最も引用される数値群が、検査の外にある。

### D-1〜D-4. 感度分析（`sensitivity.json` と文書）

| # | 項目 | 文書の記載 | **配信データの実測** | 記載場所 |
|---|---|---|---|---|
| D-1 | 仮定員 9,748 行を全部落とす（上位10） | 90% | **80%** | `CLAUDE.md:281` / `status.md:454` / `issues.md:628` |
| D-2 | 帯域群のうち特別支援学校 単独 ±30% | 71% | **75%** | `CLAUDE.md:285` / `status.md:391` / `issues.md:981` |
| D-2 | 同 他 3 層 | 87〜92% | **86〜91%** | 同上 |
| D-3 | Leave-one-out「事業所・駅・用途地域・騒音も 40%」 | 40% | **事業所 70 / 駅 80 / 用途地域 10 / 騒音 80** | `status.md:404` |
| D-3 | 騒音を外すと上位 10 の 60% が入れ替わる | LOO 40% | **LOO 80%**（入れ替わるのは 20%） | `status.md:328,424` |
| D-4 | D1 の群別表 全体 | 種別重み 96/90・仮定員 99/90・帯域 76/30・用途地域 88/40・IDW 86/70・α/β 96/90・全部 60/10 | **88.5/80・92.3/80・65/30・75.2/10・83.7/70・85.8/60・62.5/10** | `issues.md:965〜971` |

**D-4 は表ごと 1,200m 時点のもの**で、その直後の段落だけが 800m の値
（65%・最悪 30%）に更新されている。同じ節の中で世代が混在している。

**D-3 は含意が変わる。** 現ビルドで上位 10 件への影響が 2 番目に大きい層は
**用途地域（LOO 10%）** であり、騒音（80%）ではない。
`status.md` は騒音を重い層として書き、用途地域を挙げていない。
`issues.md:747` の A4 比較表には正しく「用途地域 40% → **10%** 悪化」と
書いてあるので、**issues.md の中でも節によって世代が違う。**

> **A4 の言い方に影響する。** `CLAUDE.md` は「上位の顔ぶれは**特別支援学校 1 層**で
> ほぼ決まる」と書いている。LOO は特別支援学校 0%・**用途地域 10%** で、
> 「1 層」ではなく **2 層**が握っている。用途地域を全部同じ値に潰す
> シナリオでも上位 10 の重なりは **10%**・順位変動の中央値 36 位である。
> ただし用途地域は建築基準法別表第二から導いた絶対尺度で、`AbsoluteScale` の
> `basis` を持つ——**根拠の質は帯域より高い。**「依存はしているが、
> 依存先の根拠は外部の法令にある」と書けば、不利にならずに正確になる。

### D-5〜D-8. その他

| # | 項目 | 文書の記載 | **実測** | 備考 |
|---|---|---|---|---|
| D-5 | 区別の到達不可率 | 江東 **38.8%** / 大田 **38.1%** | 江東 **38.7%**（259/670）/ 大田 **38.2%**（362/947） | `--report` の出力・独立検算の両方で一致。**値が入れ替わっている** |
| D-6 | 特別支援学校の規模の幅 | 「**29〜654 人**と **22 倍**開く」 | **22〜654 人**（愛育学園 22 / 都立王子 654）で **29.7 倍** | 最小として挙げていた 29 人（都立葛飾盲学校）は**都立の中の最小**で、**私立の愛育学園 22 人を見落としている**。倍率 22 はその 29 から導いたもので、内部では自己整合しているため気付けない。**「公立だけの調査で全件を賄おうとしない」という `CLAUDE.md` の戒めが、文書の側で破れていた** |
| D-7 | 定員が実測の事業所 | **28.7%** | **31.0%**（4,370 / 14,118） | 28.7% は 2 区時点（3,704 事業所）の値。`status.md:218` の経過記述としては正しく、`status.md:450`・`CLAUDE.md:278` の現況記述としては古い |
| D-8 | ホスト 100m 以内の組 | **595 組**（別種別 **440 組**） | **459 組**（別種別 **381 組**） | 算出コードがリポジトリに無く、**再現できない**。半径を 100〜200m で振っても 595 は出ない（150m で 546・200m で 641）。**63 組の名寄せ是正（重複行を落とす前）の値と辻褄が合う**——推測だが、459 + 63 組ぶんの近傍対 ≒ 585〜595 |

D-5・D-6 は**桁も向きも変わらない**ので結論に影響しない。だが
「江東 38.8 / 大田 38.1」のような**入れ替わり**は、この作品が
`CLAUDE.md` で繰り返し戒めている壊れ方（値の取り違え）そのものである。

D-8 は自己批判の数字なので、**多い側に間違えている**（実際より供給の
重複を大きく見積もっている）。結論を有利にする誤りではない。
ただし**再現できない数字を 5 つの文書に載せている**ことは残る。

---

## 4. 前回（2026-07-31）指摘の帰趨

| ID | 前回の指摘 | 現況 |
|---|---|---|
| **F-1** | 23 区化以降の全作業（+28,744 行）が未コミット | ✅ **解消**（`d64f180` 以降でコミット済み・作業ツリーはクリーン） |
| **F-2** | 画面が「各レイヤーはパーセンタイル順位で正規化」と誤記（騒音・用途地域は絶対尺度） | ✅ **解消**。`web/src/ui.ts:979〜1043` に層ごとの「絶対尺度」札と `basis` のツールチップ |
| **F-3** | 文書の数値が生成物と自動照合されていない | ⚠️ **仕組みは入ったが穴がある**。`tools/doc_numbers.py` ＋ CI で 10 項目を照合。ただし **`sensitivity.json` を 1 つも見ておらず**（3 章）、**渋谷駅前は誤ったメッシュを固定している**（B-2） |
| **F-5** | `proposals.json`（6 件）と画面の提言リスト（最大 12 件）で母数が違う | ✅ **解消**。`PROPOSAL_TOP_N=40` / `PROPOSAL_LIMIT=12` を `config.py` 単一の出所にし、両方 11 件で一致 |
| **F-7** | CI が無い | ✅ **解消**。`.github/workflows/ci.yml` で selftest・tsc・parity（**実データと模擬データの両方**）・doc_numbers |
| **H2** | 需要寄与の 68.9% が仮定員なのに感度分析が固定値を揺すっていない | ✅ **解消**。固定値 **76 個**へ拡張。さらに**「68.9%」自体が取り違えで、正しくは 25.1%** と判明（`d0ac05a`） |
| **H5** | 同じ数値が 6 文書に重複記載され自動照合が無い | ⚠️ F-3 と同じ。**重複記載は減っていない**（3 章の 12 件はすべて重複記載箇所の片側だけが更新されたもの） |
| **M9** | 「区内で優先度が中位以上の到達不可」が画面に出ていない | ✅ 実装済み（`ui.ts:341〜372`）。**ただし未デプロイ**（B-1） |

**前回の High 指摘は 1 つを除いて閉じている。** 残る 1 つ（F-3 / H5＝
文書の数値の重複と照合漏れ）が、今回見つかった 12 件の不一致と B-2 の
両方の原因になっている。**「規約は仕組みにする」は正しかったが、
仕組みの網が狭かった。**

---

## 5. 凍結してよい値（確定値）

以下は 2026-08-02T06:00:47Z のビルドから直接読み、独立検算で確認した。
**動画・発表資料はここから引くこと。**

### 5.1 規模

| 項目 | 値 |
|---|---|
| 対象 | 東京 23 区 / JIS X 0410 5 次メッシュ（250m）**9,507 区画** |
| 1 セルの実寸 | 約 232m × 283m |
| 実データ | **10 / 10 レイヤー**（模擬なし） |
| 需要側 | 障害福祉事業所 **14,118** / 駅 **617** / 特別支援学校 **45** / 精神科・心療内科 **1,548** |
| 負荷側 | 用途地域 **7,002** / 騒音測定点 **428**（23 区内 383）/ 都市公園 **3,835** / 混雑メッシュ **4,221** |
| 供給側 | ホスト **1,541 件**（区民センター 530・児童館 501・図書館 247・出張所 160・文化施設 76・公園管理施設 27） |
| 配信 | `mesh.geojson` 6,679,930 B（brotli -q11 で **486,112 B**）/ `demand_points.geojson` 4,552,957 B |
| コード | Python **9,761 行**（うち `selftest.py` 1,512）/ TypeScript **2,177 行** / CSS 690 行 / 文書 **4,197 行** |

### 5.2 結果

| 項目 | 値 |
|---|---|
| **到達不可** | **1,058 区画（11.13%）**。うち区内で優先度が中位以上 **151 件** |
| 提言 | **11 件**（上位 40 区画を隣接で束ねた地区） |
| 上位 10 の区内訳 | **文京 4 / 豊島 3 / 千代田 2 / 江東 1** |
| 上位 20 の区内訳 | 豊島 6 / 千代田 6 / 文京 4 / 板橋 2 / 江東 1 / 新宿 1 |
| 第 1 位 | **豊島区 大塚・北池袋周辺**（6 区画）。最上位の区画 `5339457833` は 事業所 **60 件** / 定員計 **524 人** / 大塚駅 108,702 人・137m / 商業地域 / 推定騒音 71.8dB / 緑被覆なし / 徒歩圏の公共施設 4 件・最寄り 巣鴨図書館 **658m** |
| 渋谷駅前（`5339358633`） | **792 位 / 9,507**（優先度 0.915）※ B-2 |
| 上位 50 のうち到達不可 | **0 件** |

**到達不可は半径で単調に動く**（`sensitivity.json` の `host_distance`）。
「700m」という置き方に結論が張り付いていないことを示せる:

| 徒歩圏の半径 | 到達不可 | うち区内で中位以上 |
|---|---|---|
| 500m | 2,350（24.7%） | 723 |
| 600m | 1,481（15.6%） | 323 |
| **700m（採用）** | **1,058（11.1%）** | **151** |
| 800m | 830（8.7%） | 83 |
| 1,000m | 637（6.7%） | 51 |

### 5.3 検証の到達点

| 項目 | 値 |
|---|---|
| 不変条件テスト | **63 件通過** |
| Python ↔ TypeScript | 全 9,507 メッシュで**乖離 0**（模擬データモードでも一致） |
| 構成要素間の最大相関 | **0.804**（駅 × 精神科）——0.9 未満で二重計上なし |
| VIF 最大 | **3.36**（精神科）——目安 5 を下回る |
| 主成分 | PC1 寄与率 **34.5%**（駅 −0.53 / 精神科 −0.52 / 混雑 −0.51 が同符号＝「ターミナル性」） |
| 重み ±30%（500 試行） | 上位10 **73.3%**（最悪 10%）/ 上位20 79.7% / 上位50 **86.6%**。順位変動 中央値 6 位・90%点 22 位 |
| 固定値 76 個 ±30%（60 試行） | 上位10 **62.5%**（最悪 10%）・順位変動 中央値 11 位 |
| 4 プリセット共通の上位 20 | **0 件（0%）** |

**Leave-one-out（その層を外したとき、上位 10 件のうち何件が残るか）**

| 層 | 残存 | |
|---|---|---|
| **特別支援学校（規模重み）** | **0%** | 全部入れ替わる |
| **用途地域（法的な騒がしさ上限）** | **10%** | 3 章 D-3 の注記 |
| 緑・公園被覆（減点） | 50% | |
| 障害福祉サービス事業所 | 70% | |
| 混雑（従業者数） | 70% | |
| 駅乗降規模 | 80% | |
| 精神科・心療内科 | 80% | |
| 自動車騒音 | 80% | |

**固定値の群別（±30%・上位 10 件の重なり）**

| 群 | 個数 | 平均 | 最悪 | 順位変動（中央値） |
|---|---|---|---|---|
| 定員の無い種別に当てている仮定員 | 25 | 92.3% | 80% | 0.0 位 |
| 障害福祉サービスの種別重み | 30 | 88.5% | 80% | 1.0 位 |
| 騒音の内挿（平滑化 50m・打ち切り 1500m） | 2 | 83.7% | 70% | 1.0 位 |
| 合成指数 α・β | 2 | 85.8% | 60% | 3.0 位 |
| 用途地域 → 負荷の写像 | 13 | 75.2% | **10%** | 2.2 位 |
| **徒歩圏の帯域** | 4 | **65.0%** | **30%** | 11.5 位 |
| **上の全部を同時に** | **76** | **62.5%** | **10%** | 11.0 位 |

**帯域を層ごとに単独で揺らすと**、特別支援学校 **75.3%** に対し
障害福祉事業所 86.3% / 精神科 90.5% / 駅 90.8%。
**帯域群の重さはこの 1 層から来ている。**

**仮定を除去したとき（`fixed_values.scenarios`）**

| シナリオ | 上位10 | 上位20 | 上位50 |
|---|---|---|---|
| 仮定員の 9,748 件を需要から全部落とす | **80%** | 90% | 92% |
| 定員を使わず件数だけで需要を作る | 80% | 95% | 86% |
| サービス種別を区別しない（全種別 0.50） | 80% | 100% | 88% |
| **用途地域を区別しない（13 値を全て 0.30）** | **10%** | 35% | 46% |

---

## 6. 設計の要点が、現ビルドで成り立っているか

**再導出はしない**（`docs/methodology.md` にある）。凍結検証として、
**設計上の主張が現ビルドの数値で裏付けられているか**だけを見た。

| 主張 | 検証 | 判定 |
|---|---|---|
| **掛け算モデル**（優先度 = 需要 × 負荷）。片側だけ極端な場所を上位に入れない | 上位 10 は需要 0.970〜1.000・負荷 0.969〜0.998 で**両側が同時に高い**。渋谷駅前は負荷 0.995 でも需要 0.726 で 792 位 | ✅ 成立 |
| 需要 0 の区画は優先度が厳密に 0 | `selftest` が検査（`zero_is_absence`）。配信データでも需要 0 の区画は優先度 0 | ✅ 成立 |
| 需要側と負荷側が**独立に**同じ場所を指す（駅レイヤー単独の結果ではない） | 相関 最大 0.804・VIF 最大 3.36。駅を外しても上位 10 の 80% が残る | ✅ 成立 |
| **絶対尺度**（騒音・用途地域）は対象地域を変えても値が変わらない | `selftest` が「他メッシュに依存しないこと」を検査。`meta.components` に `absolute.basis`（環境基準・要請限度 / 建築基準法別表第二） | ✅ 成立 |
| **この道具は区画について述べ、施設については述べない** | 提言 11 件の見出しはすべて「区名＋最寄り駅名」。施設名は根拠文の中で「最寄り」としてのみ登場 | ✅ 成立 |
| 提言の束ね方は**メッシュの隣接**（`grid_index`） | 11 件の `mesh_codes` が隣接集合として整合。Python と TypeScript で同じ地区名 | ✅ 成立 |
| **プリセットと重みの定義は `config.py` が唯一の出所** | `meta.json` 経由で配信。TypeScript 側に重複定義なし | ✅ 成立 |
| 順位は最も頑健な出力**ではない**。到達不可のほうが頑健 | 到達不可は重み・スコア・帯域のいずれも通らない。半径を 500〜1,000m で振っても単調（5.2） | ✅ 成立 |
| 「上位は渋谷駅前ではない」 | 792 位 / 9,507（上位 8.3%）。提言（上位 40）には入らない | ⚠️ **成立するが、3,469 位という説明では成立しない**（B-2） |
| 「上位の顔ぶれは**特別支援学校 1 層**でほぼ決まる」 | 特別支援学校 LOO 0%。**ただし用途地域も 10%** | ⚠️ **「1 層」ではなく 2 層**（D-3 の注記） |

---

## 7. データ出典と、残る弱さ

`docs/status.md`「まだ本物でないもの」と重複するが、**凍結する数値の
すぐ隣に置いておくべき**なので再掲する。件数は現ビルドで確認した
（3 章で直した値を反映してある）。ただし**「予約制の無人貸室 103 件」と
「地域活動支援センター 17 件」は配信データから直接は数えられない**——
前者は `区民センター` に、後者は投入前に落とされていて残らないため、
`docs/issues.md` の記載による。

| レイヤー | 出典 | 年次 | 残る弱さ |
|---|---|---|---|
| 障害福祉事業所 14,118 | WAM NET 情報公表システム | 2026-03 | **定員が実測なのは 31.0%**（訪問系・相談系・共同生活援助は制度上定員が無い）。**地域活動支援センター 17 件は対象外で落ちている**（過小計上） |
| 特別支援学校 45 | 国土数値情報 P29（位置）＋ 都教委 学校別在籍者数（規模） | 2023 / 2025 | **42 校が実数（22〜654 人・計 9,926 人）、国立 3 校は非公表で既定値 150 人。** 基準日が層の内部でずれる |
| 駅 617 | 国土数値情報 S12 | 2024 | — |
| 精神科・心療内科 1,548 | 国土数値情報 P04 | 2020 | 標榜科目ベース |
| 用途地域 7,002 | 国土数値情報 A29 | 2019 | **LOO 10%**——結論への影響は特別支援学校に次ぐ |
| 騒音 428 点 | 東京都環境局 自動車交通騒音調査 | **2013** | **幹線道路の道路端しか測っていない。**23 区内 383 点が区ごとに 5〜31 点と偏る。**観測圏外 1,095 セルを中央値で補完**（`issues.md` A6・未解決） |
| 都市公園 3,835 | 国土数値情報 P13 | **2011** | — |
| 混雑 4,221 メッシュ | e-Stat 経済センサス（従業者数） | 2021 | **昼間人口ではない。**買い物客・通学者を含まない |
| ホスト 1,541 | P14 児童館 ＋ 23 区の公共施設一覧 | 2022 / 2025 | **都・国・民間は入っていない。北区だけ中身が薄い**（22 件中 20 件が児童館・図書館 0 件）。**100m 以内に別の行が並ぶ組が 459 組**（D-8）。**予約制の無人貸室 103 件を図書館と同格に数えている** |

**年次がそろっていない**（事業所 2026 / 駅 2024 / 混雑 2021 / 用途地域 2019 /
騒音 2013 / 公園 2011）。**当事者による検証を経ていない。**

---

## 8. 提出後の宿題（いま触らない）

いずれも**モデルか結論を動かす**ため、凍結の外に置く。
判断の経緯は `docs/issues.md` に残っている。

| | 内容 | いま触らない理由 |
|---|---|---|
| **A4** | 特別支援学校を需要側から外す（点が 45 校と疎で、帯域がそのままゲートになる） | 提言が全部入れ替わる。`issues.md` A4「対処方針」で **2026-08-01 に「直さず明記する」と決定済み** |
| **A6** | 騒音の観測圏外 1,095 セルの補完（中央値 → 別の方法） | 順位を動かさないと実測済み |
| **A5 / D4** | 供給側の点検（同じ建物の別種別 459 組、予約制の無人貸室 103 件） | 到達可否を動かさない |
| **新規** | `HOST_PREFERENCE` の撤去（A10 以降、順位に効いていない） | 死んだ設定だが、消すと生成物が変わる可能性がある |

---

## 9. 推奨する凍結手順

**上から順に。1〜2 を終えるまで資料の数値を確定しないこと。**
**4 項目とも監査と同日に実施済み**（10 章）。

1. **B-2 を直す**（順序が重要——文書を先に直すと誤った値で固定される）
   - `tools/doc_numbers.py:74` の `5339358611` → `5339358633`
   - `python tools/doc_numbers.py` が **792 位**を要求することを確認
   - `docs/status.md:21,360` / `CLAUDE.md:300` / `docs/methodology.md:395,404,1210` を
     **792 位・優先度 0.915** へ。`methodology.md` の比較表は B-2 の表へ差し替え
   - **経過として残してある過去の順位（85 位・79 位・519 位・642 位）は消さないこと。**
     いずれも `5339358633` の値で、履歴として正しい。**消すのは 3,469 位だけ**
     ——これは一度も渋谷駅前の値だったことが無い
2. **B-1 を直す** — `web/` で `npm run deploy`。
   公開 `meta.json` の `generated_at` が `2026-08-02T06:00:47+00:00` になり、
   `unreachable` が入り、提言が 11 件になることを確認。
   `docs/handoff-hosts.md:9` の「未反映」を消す
3. **3 章の D-1〜D-8 を直す**（12 箇所。すべて記載場所を特定済み）
4. **検査の網を広げる**（F-3 / H5 を閉じ切る）
   - `doc_numbers.py` に `sensitivity.json` 由来の値を追加する
     （**実施済み。8 項目を追加して計 18 項目**）
   - **数値を書く文書を減らす**ほうが本質的だが、提出後でよい
5. **このレポート自身の扱いを決める**（次節）

### このレポートが同じ罠に落ちないために

前回のレポートは **3 日で古くなった。** 原因は 2 つある。

- **どの文書からもリンクされていない。** `CLAUDE.md` の「読む順番」にも
  `README.md` にも出てこないので、再開時に誰も開かない
- **同じ内容が `.md` と `.html` に 2 つある**（この作品が繰り返し踏んでいる
  「同じものが 2 箇所にある」型）

そこで今回は **HTML を `.md` からの生成物にした**（`tools/audit_html.py`）。
手で両方を保つのをやめる。加えて**冒頭に対象ビルドの `generated_at` を明記**し、
配信データが変わればこのレポートが対象外になることを読み手が判定できるようにした。

**このレポートは 2026-08-02T06:00:47Z のビルドについての記述である。**
再ビルドしたら、付録 A の手順で採り直すこと。

---

## 10. 是正の記録（2026-08-03・監査と同日）

**B-1 以外はこのセッションで直した。** 配信データ（`web/public/data/`）は
**1 バイトも変えていない**——変えたのはコードと文書だけである。

### 直したもの

| # | 内容 | 変更箇所 |
|---|---|---|
| **B-2** | 渋谷駅前のメッシュを `5339358611`（代官山）→ **`5339358633`**（渋谷駅）へ。順位 **3,469 位 → 792 位**。**検査はコードだけでなく中身で裏を取る**ようにした（最寄り駅「渋谷」・用途地域「商業地域」が一致しなければ例外で止まる） | `tools/doc_numbers.py` / `docs/status.md` / `docs/methodology.md` / `CLAUDE.md` |
| **D-1** | 仮定員を全部落としたときの上位10 **90% → 80%** | `CLAUDE.md` / `status.md` / `issues.md` |
| **D-2** | 帯域単独の特別支援学校 **71% → 75.3%**、他 3 層 **87〜92% → 86〜91%** | 同上 |
| **D-3** | Leave-one-out を現ビルドの 8 層すべてで表にした。**「1 層ではなく 2 層」**（特別支援学校 0% / 用途地域 10%）を明記 | `status.md` / `CLAUDE.md` |
| **D-4** | D1 の群別表を 800m の値へ。右端に 1,200m の頃を残し、**測り直しを忘れた事実そのものを記録**した | `issues.md` |
| **D-5** | 区別の到達不可率 江東 **38.8→38.7%** / 大田 **38.1→38.2%** | `status.md` / `issues.md` / `methodology.md` / `handoff-hosts.md` / `README.md` |
| **D-6** | 特別支援学校の規模 **22〜654 人・29.7 倍**（最小は私立の愛育学園） | `status.md` / `issues.md` / `methodology.md` / `CLAUDE.md` |
| **D-7** | 定員が実測の事業所 **28.7% → 31.0%**。2 区時点の記述には「2 区時点の値」と明記 | `status.md` / `methodology.md` / `CLAUDE.md` |
| **D-8** | ホスト 100m 以内の組 **595 → 459 組**（別種別 440 → 381）。**`--report` で出るようにした**（`etl/hosts.py` の `proximity_report`） | `etl/hosts.py` / `etl/build.py` ＋ 文書 5 本 |

### 仕組みを直したもの（同じことが起きないように）

- **`doc_numbers.py` の網を 10 → 18 項目へ。** `sensitivity.json` を検査対象に
  加えた（重み ±30% の上位10 / 固定値 76 個 / 帯域群 / 特別支援学校の LOO /
  用途地域の LOO / 仮定員を落としたとき / プリセット共通 / 帯域の単独）。
  **12 件の不一致はすべて、ここが空白だったために起きている。**
- **丸めを `score.publish_round` と同じ規則に揃えた。** 組み込みの `round` は
  75.25 を 75.2 へ（偶数側へ）丸める。文書に書く値と検査が食い違う
- **`proximity_report` を足した。** D-8 の原因は値の誤りではなく
  **算出コードが残っていなかったこと**で、数字だけ直しても再発する
- **HTML をこのレポートからの生成物にした**（`tools/audit_html.py`）。
  前回 `.md` と `.html` を手で 2 つ保って両方古くなったため

### 是正後の検証

| | |
|---|---|
| `python -m etl.selftest` | ✅ **63 件** |
| `node tools/parity_check.mjs`（実データ） | ✅ 乖離 0 |
| `node tools/parity_check.mjs`（模擬データ） | ✅ 乖離 0 |
| `npx tsc --noEmit` | ✅ 0 件 |
| `python tools/doc_numbers.py` | ✅ **18 項目すべて一致** |
| `git status web/public/data/` | ✅ **変更なし**（配信データは凍結したまま） |

### B-1 も同日に解消した

`web/` で `npm run deploy` を実行し（9 ファイルを更新）、公開先を確認した。

| | |
|---|---|
| 公開 `meta.json` | **手元と完全一致**（`generated_at` = 2026-08-02T06:00:47+00:00） |
| 公開 `unreachable` | **`{count: 1058, ratio: 0.1113, mid_or_above: 151}`**（それまで存在しなかった） |
| 公開 `proposals.json` | **11 件・手元と完全一致**（第 1 位 豊島区 大塚・北池袋周辺） |
| 画面 | 見出しに **1,058 区画 / 11.1%**、提言 11 件、コンソールエラーなし |

**これで合格条件 3 つがすべて満たされた。**
コード・配信データ・文書・公開サイトが 1 つのビルド
（`2026-08-02T06:00:47+00:00`）で揃っている。

---

## 付録 A. この監査の再現手順

```bash
cd ~/Dev/calmgap-tokyo
source .venv/bin/activate

# 1. 検証
python -m etl.selftest          # 63 件
node tools/parity_check.mjs     # 全 9,507 メッシュで乖離 0
(cd web && npx tsc --noEmit)
python tools/doc_numbers.py

# 2. 配信データからの独立検算（etl を通さない）
python - <<'PY'
import json, pathlib, collections, statistics
D = pathlib.Path('web/public/data')
meta = json.loads((D / 'meta.json').read_text())
rows = [f['properties'] for f in json.loads((D / 'mesh.geojson').read_text())['features']]
W = meta['target_wards']
for r in rows:
    r['_w'] = W[r['w']]; r['_un'] = 'host' not in r
order = sorted(rows, key=lambda r: -r['priority'])
rank = {r['c']: i + 1 for i, r in enumerate(order)}
un = [r for r in rows if r['_un']]
print('メッシュ', len(rows), '/ 到達不可', len(un), f'{100*len(un)/len(rows):.2f}%')
by = collections.defaultdict(list)
for r in rows: by[r['_w']].append(r)
print('うち区内で中位以上', sum(
    1 for w, rs in by.items()
    for r in rs if r['_un'] and r['priority'] >= statistics.median([x['priority'] for x in rs])))
print('上位10', collections.Counter(r['_w'] for r in order[:10]).most_common())
print('上位20', collections.Counter(r['_w'] for r in order[:20]).most_common())
print('上位50のうち到達不可', sum(1 for r in order[:50] if r['_un']))
print('渋谷駅前 5339358633', rank['5339358633'], '位')  # 5339358611 は代官山。B-2
PY

# 3. 再ビルドが配信データと一致するか（凍結の前提）
cp -r web/public/data /tmp/frozen
python -m etl.build --live --report
for f in mesh.geojson proposals.json cards.json hosts.geojson demand_points.geojson; do
  cmp -s /tmp/frozen/$f web/public/data/$f && echo "同一 $f" || echo "差分 $f"
done
git checkout -- web/public/data/     # 凍結時のバイト列へ戻す

# 4. 公開先が同じビルドか
curl -s https://calmgap-tokyo.hiroaki-kuwabara.workers.dev/data/meta.json \
  | python -c "import json,sys; m=json.load(sys.stdin); print(m['generated_at'], m.get('unreachable'))"
```

## 付録 B. 確定値（2026-08-02T06:00:47Z ビルド）

| 指標 | 値 |
|---|---|
| 対象 | 東京 23 区 / 5 次メッシュ（250m）**9,507 区画** |
| 実データ | **10 / 10 レイヤー** |
| 需要側 | 事業所 14,118・駅 617・特別支援学校 45・精神科 1,548 |
| 負荷側 | 用途地域 7,002・騒音 428 点・公園 3,835・混雑メッシュ 4,221 |
| 供給側 | 1,541 件（区民センター 530・児童館 501・図書館 247・出張所 160・文化施設 76・公園管理施設 27） |
| **到達不可** | **1,058 区画（11.13%）**。うち区内で優先度が中位以上 **151 件** |
| 提言 | **11 件**。第 1 位 豊島区 大塚・北池袋周辺（6 区画） |
| 上位 10 の区内訳 | 文京 4 / 豊島 3 / 千代田 2 / 江東 1 |
| 上位 20 の区内訳 | 豊島 6 / 千代田 6 / 文京 4 / 板橋 2 / 江東 1 / 新宿 1 |
| 上位 50 のうち到達不可 | 0 |
| **渋谷駅前**（`5339358633`） | **792 / 9,507**（優先度 0.915） |
| 感度（重み ±30%・500 回） | 上位10 **73.3%**（最悪 10%）・上位50 86.6%・順位変動 中央値 6 位 |
| 感度（固定値 76 個 ±30%・60 回） | 上位10 **62.5%**（最悪 10%） |
| Leave-one-out 最小 | **特別支援学校 0%**・次いで**用途地域 10%** |
| プリセット共通 | 4 プリセット全てで上位 20 に入る区画は **0 件** |
| テスト | `selftest` **63 件**・`parity_check` 全メッシュで乖離 0 |
| コード | Python 9,761 行 / TypeScript 2,177 行 / CSS 690 行 / 文書 4,197 行 |
| 配信 | `mesh.geojson` 6.4MB（brotli -q11 で 475KB）・`demand_points.geojson` 4.3MB |

---

*本レポートは `generated_at = 2026-08-02T06:00:47+00:00` の配信データと、
コミット `501caed` のリポジトリ状態に基づく。数値は再ビルドで変わり得る
（別環境では `issues.md` B4 の 4 桁目の差が出る）。引用する際は付録 A で採り直すこと。*
