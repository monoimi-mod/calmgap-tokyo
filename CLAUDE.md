# calmgap-tokyo で作業するときに

東京都のカームダウン・クールダウンスペース設置優先度を、オープンデータから
算出して根拠つきで提言するツール。都知事杯オープンデータ・ハッカソン提出作品。

読む順番: この文書 → [`docs/status.md`](docs/status.md)（現況と再開手順）→
[`docs/issues.md`](docs/issues.md)（**未解決の偏りと不透明さ**）→
[`README.md`](README.md)（使い方）→ [`docs/methodology.md`](docs/methodology.md)（算出方法と根拠）。
[`docs/reproducibility.md`](docs/reproducibility.md)（**どこまで自動でどこから人手か**）は
「他の自治体でも回せるのか」への回答で、方法を主役に置く以上そこが主張の一部になる。
[`docs/operation-idea.md`](docs/operation-idea.md)（運営案）と
[`docs/implementation.md`](docs/implementation.md)（社会実装・導入経路）だけは
根拠の種類が違う——**データから導いたものではなく作者の提案**。
他の文書と混ぜないために分けてある。**この 2 本もさらに分けてある**——
運営方式は施設が決めること、導入経路は自治体が決めることで、
混ぜると誰の判断の話か分からなくなる。**`implementation.md` は
確定／仮説／実証課題の 3 層に分けて書く**（当事者検証を短期で作らない
判断もそこにある。2026-08-13）。
**`docs/status.md` が作業再開の起点**で、そこに残作業と「まだ本物でないもの」が書いてある。

対象は **東京 23 区**・5 次メッシュ（250m）**9,507 区画**。実データ 10/10 レイヤー。
公開先: https://calmgap-tokyo.hiroaki-kuwabara.workers.dev
（作者個人の Cloudflare アカウント。`web/` で `npm run deploy`）

**実データが 10/10 揃ったことは、数値が確からしいことを意味しない。**
`docs/issues.md` に、結論そのものを動かしている偏りが列挙してある。
ここを潰すのが現在の主作業。A1（騒音）・A7（用途地域）・A8（エッジ効果）・
A9（供給側の欠測）は是正済み。**A1 は 2026-08-04 に測定設計の側にも手が入った**
（出典を要請限度 383 点 → 常時監視 5 年分 768 点へ。副産物として A6 の
欠測セルが 1,095 → 496 に減った）。**2026-08-05 に方針を戻し、要請限度を
入れた**（1,058 点・欠測 458 セル。A1 の指標は +0.133 → +0.249 と悪化したが、
「実際にうるさいと申し立てが出た道路」の情報を採るという判断）。
**令和6年度は環境GIS＋（国立環境研究所）から補っている**——都のサイトに無い年度だけを採る（`config.NOISE_GIS_YEARS`）。**A9 の是正で新しく立った項目が 2 つある**
（予約制の無人貸室を数えていること・同じ建物を複数のホストとして数えていること）。

## 環境

`python -m etl.*` はリポジトリのルートで、`npm` は `web/` の中で実行する。

```bash
source .venv/bin/activate      # geopandas pandas numpy shapely pyproj requests
```

## 変更したら必ず通す

```bash
python -m etl.selftest           # 77 件。不変条件と「間違いが黙って通らないこと」
node tools/parity_check.mjs      # Python と TypeScript のスコア一致（全メッシュ）
node tools/facility_parity.mjs   # 数えた件数と、地図に光る点の数の一致（全メッシュ・6 群）
python tools/doc_numbers.py      # 文書と提出物の数値が配信データと合っているか
python tools/link_check.py       # 出典リンクが「そのデータのある場所」を指しているか
```

`parity_check.mjs` は `web/public/data/` を読む。**模擬・実データの両モードで
通ること**を確認する（過去に模擬モードだけ落ちていたのを見落とした）。

```bash
python -m etl.build            # 模擬データ（ネットワーク不要）
python -m etl.build --live     # data/processed の実データを使う
python -m etl.build --live --report        # 上位メッシュ・相関・VIF・主成分・区依存・到達不可
python -m etl.build --live --sensitivity   # 重み 8 個と、重み以外の固定値 76 個
```

## このプロジェクト特有の判断

**掛け算モデル。** 優先度 = 需要 × 負荷。足し算にしてはいけない。片側だけ極端な
場所を上位に入れないための設計で、`selftest` が「需要 0 なら優先度 0」を検査している。

**提言の組み立ても Python と TypeScript に 2 つある。** `etl/hosts.py` の
`build_ranking`・`cluster_adjacent` と `web/src/ui.ts` の `rankingRows`・
`web/src/area.ts`。`parity_check` の対象外なので食い違っても静かに通る。
片方を触ったら必ず両方（`selftest` が Python 側を検査する）。

**「数えたもの」と「光らせるもの」は同じ座標で判定する。** 画面は
「徒歩圏に事業所 60 件」と書いた隣で、その 60 点を地図に光らせる。
**表示される点の数と表の数字が一致しなければ、ハイライトは説明ではなく矛盾になる。**
ブラウザで緯度経度から距離を測ると 800m の境界付近でズレるので、
Python が数えるのと同じ平面直角座標（EPSG:6677）を `x`/`y`・`mx`/`my` として配信し、
**Python 側も配信するのと同じ丸めた座標で数える**（`config.PUBLISH_XY_DECIMALS`）。
丸めていない座標で数えて丸めた座標を配ったときは、境界に載る 3 点が
内と外をまたいで実際にズレた（Python 13 件・ブラウザ 16 件）。
`tools/facility_parity.mjs` が全 9,507 区画 × 6 群で一致を検査する。

**実数を数える半径は、その層がスコアに使っている帯域と同じ値にする。**
揃えるべきは数字ではなく規則である。駅は 2026-08-04 まで
「1,500m 以内の最寄り 1 駅」しか出しておらず、**スコア（帯域 600m の
カーネルで周囲の全駅を足す）とは別のものを画面が指していた**。
「他と合わせて 800m」にすると、帯域でもカットオフ（1,200m）でもない
**4 つ目の恣意的な数字**が増えるので採らない。
**最寄り 1 駅は残す——役割が違う**。順位表の見出し「豊島区 大塚」の
「大塚」がそれで、**区画の呼び名**であって需要の実数ではない
（600m に揃えると 4,197 区画・44.1% が名前を失う）。
**`f_host_n` の 700m だけは帯域でもない**——スコアに一切入らず、
「徒歩圏に 1 件も無い＝到達不可」という主張の定義そのもの。
種類が違うので揃えず、**揃っていない理由を画面のその行の隣に書く。**

**騒音の測定点も同じ規則で扱う。** `f_noise_n` は徒歩圏ではなく
**IDW の打ち切り距離（`config.NOISE_IDW_MAX_DISTANCE_M` = 1,500m）**内の
測定点の数で、`facility_parity` の 6 番目の群として全区画で照合する。
**0 件は「静か」ではなく「測っていない」**——その区画の `f_noise_db` は
23 区の中央値で、画面はそう書く（458 区画。`issues.md` A6）。
だから **「0 件」と「IDW の観測圏外」が同じ区画集合であることを
ビルドが毎回確かめる**（`build._check_noise_point_count`）。ずれ得るのは
座標の丸めだけ（IDW は丸めない座標・件数は配信する丸めた座標）。
**測定点は施設ではない**ので、需要側の点と別ファイル・別の描き分け
（白抜き）にする。新しい色相は作らない。

**一覧の行から地図の吹き出しまで開く。** 寄せるだけでは足りなかった
（2026-08-05）——250m 四方に 60 件が重なる場所では、寄った先のどの点が
押した施設なのか分からない。**渡すのは配列の添字で、緯度経度ではない**
（同じ地点に 2 件あると別の行の中身が出得る）。吹き出しの中身は
**地図の点を押したときと同じ関数**（`pointDetailHtml`）を通す。

**その 60 件は一覧でも出す**（2026-08-04。250m 四方に 60 件が重なる場所では
点を押すこと自体が現実的でない）。**一覧は `highlightFor()` が返した配列
そのものから作り、数え直さない。** ここで絞り直すと
「表の件数・地図の点・一覧の行数」が 3 者ばらばらにずれ得るものになる。
同じ配列を配れば構造的にずれない——`facility_parity` は前 2 者しか
検査しておらず、**一覧が 3 つ目の食い違い先になる余地を作らない**。

**出典の名前は `config.py` が唯一の出所。** ただし
`data/processed/*.geojson` の `source` 列は**正規化した時点の label を
焼き付けたスナップショット**なので、label を直しても古い名前が残る。
実際、公共施設一覧の label を直したとき、出典一覧は新しい名前・
施設をクリックすると古い名前、という**同じ出典が 2 つの名前を名乗る**
状態になった（一覧を出して初めて見えた）。`Source.aliases` に旧 label を
書き、読む側で寄せる（**`config.current_source_label` が唯一の出所**）。
**画面のためだけに再正規化しないこと**——名前を直すたびに 30 本を
読み直すことになり、出典名の出所が config でなくなる。

**ただし `aliases` を知らない経路があると、再正規化が黙って効かなくなる。**
`fetch.py` の `--append` は「入れ直した出典の古い行を先に捨てる」——
これが**現在の label しか見ていなかった**ため、公共施設一覧を改名して以降
**除外パターンを足して再正規化しても出力が 1 バイトも変わらない**状態に
なっていた（古い行が「別の出典」として残り、新しい行が「同名かつ 100m 以内」で
全部消える。**しかも「1,541件を書き出した」と成功と表示される**）。
2026-08-13 に是正。**同じ事故を防ぐために書いたコードが、改名でもう一度
開いていた**——判定を 2 箇所に持つと、片方だけ古くなる。

**出典の名前は、著作権表示の代わりにならない。** CC BY が最初に求めるのは
**作者名**で、`label`（データの名前）でも `license`（条件）でもない。
公共施設一覧は `license="東京都オープンデータカタログ CC BY 4.0"` と書いてあり、
**それらしく見えるので見て気付ける位置になかった**——実際の著作者は 23 の区
それぞれで、しかも読んだファイルの一部（北区・足立区・渋谷区・目黒区の新しい版）は
**そのカタログに載っていない**。**カタログの表記を、カタログに無いものにまで
名乗らせていた**（2026-08-13 に是正。`issues.md` C6）。C5 で「カタログの
CC BY 4.0 をそのまま名乗ってはいけない」と書いた規則を、別の出典で破っていた。
`rights_holder`（作者名）と `license_url`（条文）を欄として分け、
**`selftest` が空欄を止める**——欄を作っただけでは次に足す出典で静かに戻る。
**逆向きにはしない**（URL が無ければ落とす、にしない）——環境局の騒音は
口頭の許諾で指せる条文が無く、一律に埋めさせると**確かめていない条件まで
断言する**ことになる。空欄の理由は `license` の本文に書く。

**確認の記録は [`docs/handoff-license.md`](docs/handoff-license.md) にある**
——23 区すべてのライセンスを API で数えた結果と、カタログに無いファイルを
使っている 4 区の出所。**同じ確認を次にやり直さないため**に残してある。

**素のデータを出していないなら、そう書く。** 改変した旨の明記は CC BY・
国土数値情報の利用約款（PDL1.0）・e-Stat 利用規約がそろって求めている条件で、
この作品は名寄せ・除外・座標化・内挿・メッシュ集計・重み付き合成を通している。
文言は `config.MODIFICATION_NOTICE` が唯一の出所で、**出典表の上に置く**
——表を読んだあとで但し書きが来ても遅い。

**同じ数式が Python と TypeScript に 2 つある。** 重みスライダーのため。
`etl/score.py` の `compose()` と `web/src/score.ts` の `compose()` は
同一でなければならない。片方を触ったら必ず両方。丸めも揃える
（`score.publish_round` は JavaScript の `Math.round` と同じ規則。
Python 組み込みの `round` は「ちょうど半分」を偶数側へ丸めるので使わない）。

**並び順はスコアに触れてはいけない。** `_weighted_sum` は**キー順に足す**
（定義順ではない）。浮動小数の加算は順序で最後の桁が変わるので、定義順のまま
足すと**画面の並びを変えただけでスコアが動く**——実際そうなっていて、
需要側の並びを変えたとき 29 区画の優先度が 4 桁目でずれた。
`config.py` の並びは**画面の見やすさのために決める**もので、そこを触るたびに
数値が動くのでは並べ替えを検討することすらできない。**TypeScript 側も
同じ順に並べ替える**（片方だけだと `parity_check` が落ちる）。
`selftest` が「並び順を逆さにしても結果が完全一致すること」を検査する。

**「レイヤー」は 10 と 8 の 2 通りに数える。両方書いて、対応も書く。**
データの層は 10（`layer_provenance`）、スコアに入るのは 8（`ALL_COMPONENTS`）。
どちらも正しいのに対応が画面のどこにも無く、**外部から「8 なの 10 なの、
意味が分からない」と言われた**（2026-08-05）。**数の食い違いは片方を
書き換えて消すものではない**——スコアに入らない 2 層のうち `hosts` は
「到達不可」というこの作品でいちばん頑健な出力を作っており、
**スコアに入らないことがそのまま長所**である。`Component.layer` と
`config.SUPPORT_LAYERS` を `meta.layer_roles` として配信し、画面はそこから
数を出す（画面に直接書かない）。**分類漏れは `build._layer_roles` が止める**
——書き忘れると、画面の「10 層のうち 8 層」だけが黙って古くなる。

**層の正規化値は重みで動かない。動く量を画面に出す。** スライダーを
動かしても内訳の数値が動かないのは直感に反する、という指摘を受けた
（2026-08-05）。**動かないのは正しい**——層の値はその区画の性質で、
こちらが何を重視するかとは無関係である。**だが画面はそれを言っておらず、
動く量もどこにも無かった。** 各行に「寄与＝正規化値 × 重み」と
その側での寄与割合を出し、動かない理由を節の頭に書く。
**正規化値そのものを重みで動かしてはいけない**——そうすると
「この区画に事業所が何件あるか」が重みで変わることになる。

**「地域内順位」と書くなら、順位も書く。** 母数（3,626 区画中）だけを
出して順位を出していなかった（2026-08-05・指摘されるまで気付かず）。
順位はブラウザが配信された `n_*` から作る（`computeLayerRanks`）——
別の値から作ると札の言う順位と画面の値が食い違い得る。
**絶対尺度の層では順位を主にしない**。その層の値を決めているのは
法令の物差しであって順位ではないので、順位は「23 区の中でどのあたりか」を
言うだけの**参考**として物差しの後ろに置く。

**レイヤーを並べる場所は 4 つある。順序は `config.py` の定義順で揃える。**
左のスライダー・算出方法とデータ出典・需要側／負荷側の内訳・
「レイヤー別」タブの層の選択。
かつて内訳だけ**寄与の大きい順**で、実数は**半径ごと**の別節にあり、
**同じ 8 層が画面の 2 箇所に別の順序で出ていた**（2026-08-04 に是正）。
寄与順は一見親切だが、**区画を選び直すたびに行が入れ替わって
区画どうしを見比べられない**。大きさは棒が示すので順序に情報は要らない。
**実数は内訳の行そのものに畳む**——「事業所 60件」と「事業所 0.995」を
離して置くと、どちらがどちらの根拠なのか読めない。
**重み 0 の層も消さない**（消すと「測っていない」と読める）。
**スコアに入らない値（公共施設・区画の呼び名）は内訳に混ぜない**——
同じ見た目で並べると 9 番目・10 番目のレイヤーに見える。

**プリセットと重みの定義は `etl/config.py` が唯一の出所。** `meta.json` 経由で
配信する。TypeScript 側に重複定義を作らない（画面と感度分析が食い違っても
誰も気付けない）。構成要素の正規化も Python 側で完結し、TypeScript は
配信された `n_*` を重み付き加算するだけ。

**優先度は「需要 × 負荷」の生値。順位化していない。** かつては合成の後に
もう一度パーセンタイル順位へ変換して配信していたが、`methodology.md` の式は
そう書いておらず、**文書と実装が食い違ったまま実装側が画面に出ていた**
（2026-08-03 に除去。順位は全 9,507 区画で不変）。**順位化をやめると、
順位化を前提にした計算が黙って壊れる**——根拠文の「上位◯%」を
`(1 - priority) × 100` で出していたため、渋谷駅前が 792 位（上位 8%）なのに
28% と出るようになった。順位から出すよう Python・TypeScript の両方を直してある。
**需要と負荷にはまだ順位化が 1 回ずつ残っている**（`issues.md` B5）。

**順位化は分布の広さを捨てる。** パーセンタイル正規化は、その層が対象地域内で
どれだけ狭い範囲に収まっていても必ず 0〜1 いっぱいに引き伸ばす。
**識別力の無い層ほど差が誇張され、測定の偏りがあればそれごと増幅される。**
騒音（実測幅 2.24dB が σ 0.289 へ）と用途地域（設計値 0.05 が 0.285 へ）で
実際に起きた。0 と 1 を外部の法令・告示で決められる層は `AbsoluteScale` を使う
（`basis` にどの法令から取ったか書けない層に使ってはいけない。
順位化の恣意性を別の恣意性に置き換えるだけになる）。

**感度分析の速い経路は、本番と同じ式を通す。** 重み以外の固定値を揺さぶる
`sensitivity.FixedValueModel` は `build_mesh_table` を毎回呼ばず、
カーネルの三つ組（`aggregate.kernel_triplets`）と面積シェア
（`polygon_area_shares`）を使い回す（1 試行 24 秒 → 20 ミリ秒）。
**`points_to_mesh` もこの三つ組を呼ぶ**——速い経路に式をもう 1 つ書くと、
感度分析が本番と違う式で「変わりません」と言うことになり、
その数字が一番信用できないものになる。`selftest` が
「何も差し替えなければ生値が完全一致すること」を検査している。

**「仮定員かどうか」を値の一致で判定しない。** `welfare.geojson` の
`capacity_assumed` は**正規化の時点で「定員欄が空だったか」を記録した列**。
値の一致（`capacity == assumed_capacity(kind)`）で後から復元すると、
**実定員がたまたま仮定員と同じ 20 人だった行 2,934 件**を仮定側に数える
（仮定員 20 人はその実態に寄せて置いた値なので、一致するのは当たり前）。
この取り違えで「需要寄与の 68.9% が仮定員」と書いていたが、実際は **25.1%**。
**行数の割合 69.0% と数字が近く、取り違えても気付けない位置にあった。**

**取りこぼしても混入させない。** P14 は障害福祉と高齢者介護を分離できないため、
判断できないものは落とす方針。過小計上であることを明記して使う。
需要側は WAM NET（届出の種別・実定員）へ入れ替えたが、同じ方針で
地域活動支援センター 17 件（WAM NET の対象外）は落としている。

**この道具は区画について述べる。施設については述べない。** 需要も負荷もメッシュの
属性から作っており、**施設の適性を測る構成要素は一つも無い**。だから提言の見出しは
区画（区名＋最寄り駅名で位置を示す）で、施設名ではない。かつては割当先の施設名を見出しにして
「この施設に置け」まで言い切っていたが、**上位が図書館ばかりに見えたのは
区の一覧に何が載っていたかと例示順（`HOST_PREFERENCE`）の積で、選定の結果ではなかった**
（`issues.md` A10）。供給側から言えるのは `f_host_n`（徒歩圏 700m の件数）と到達可否だけ。
**提言の単位は区画そのもの**で、束ねない（`mesh.grid_index` による隣接は
「接している上位区画の数」を述べるためだけに使う。整数座標なので
Python と TypeScript でズレない）。

**順位はこの作品でいちばん頑健な出力ではない。** 上位の顔ぶれは
**特別支援学校 1 層**でほぼ決まる（外すと上位 10 件が 0% しか残らない。
23 区に 45 校しか無く、帯域がそのままゲートとして働くため。`issues.md` A4）。
規模を実数にしても帯域を 800m へ直しても消えなかったので、
**直さず限界として明記する方針を採った**（2026-08-01 決定。
需要側から外すのが構造的な解だが、提言が全部入れ替わるので提出後）。
**ただし依存しているのは 1 層ではなく 2 層**——用途地域を外しても
上位 10 件は 20% しか残らない。**こちらは建築基準法別表第二から導いた
絶対尺度で `basis` を持つ**ので、「依存はしているが依存先の根拠は
外部の法令にある」と言える。帯域（目安しか無い）とはそこが違う。
**いちばん頑健なのは到達不可**——重みもスコアも帯域も通っていない。
**ただし頑健なのは 1,058 のほうで、見出しに出している 148 ではない**
（148 は「区内で中位以上」の側から重みが入る。上記の訂正を参照）。

**現況の数値は `docs/status.md` に書く。** `tools/doc_numbers.py` が
配信データと突き合わせ、CI で落ちる。**「過去の数字を引用しないこと」を
機械的に守る仕組みが無かった**ため、短期間に 4 回踏んだ（selftest 45→58 件、
到達不可の中位以上 128→151 件、仮定員 68.9%→25.1%、感度分析の帯域ラベル）。
どれも間違っていても動く。**古い値が残っていないかは検査しない**——
この作品は経過の数値を意図的に残しており、機械的に禁じると
経過の記録と消し忘れが区別できず偽陽性だらけになる（試したら 17 件中 16 件が偽陽性）。

**提出物（`docs/slides.html`・`presentation.md`・`submission-form.md`）は
向きが逆である。** `status.md` は「現在値がすべて書かれていること」を求めるが、
**資料は数値の一部しか引かない**ので、全部を求めると
「区平均の開き 4.0dB がスライドに無い」で落ちる。必要なのは
**その資料が引いている数値が、いまのビルドと合っているか**で、
資料ごとに「載っている値」を明示的に並べる（`SUBMISSION_CHECKS`・71 項目）。
**資料から主張ごと消したときだけ、一覧からも消すこと。**
**配信データから出せない数を資料に書かない**——手で測った
「プリセットで 133〜171 件」は検査できないのでスライドから外した
（画面も同じ理由でこれを出していない）。**出したいなら先に ETL から配信する。**

**スライドは「見るもの」、`presentation.md` は「喋るもの」。** スライドに置くのは
短いタイトル・大きな数字・図・画面・動画と最小限の補足だけで、
**読ませる段落を作らない**（2026-08-17・ユーザー判断）。説明は台本へ移す。
**審査員が読む文と読み上げる文にダッシュ（——）を使わない**
——話し言葉として不自然で、生成 AI が書いた文章らしさが強く出る。

**並びは「何を作ったか」から始める。** 問題提起 → 方法論 → 結果 ではなく、
**何を作ったか → なぜ価値があるか → どう実現したか → 実際に使う →
条件を変える → 何が分かったか → 社会実装**（2026-08-17・ユーザー判断）。
**問題を大げさに語るより、作ったものの価値をスマートに見せる。**

**証明していない因果を課題にしない。** 「なぜ置き場所を決められないのか」は
**「決められないから設置されない」という因果**を、「逃げ場が要るのは移動している
最中」は**移動中と滞在中の比較**を、それぞれ証明していないのに主張していた
（どちらも 2026-08-17 に削除）。言えるのは
**「東京全体を見渡して比べる工程が無い」**まで。**既存の設置方法を批判しない**
——この作品は補助線を引くものである。

**駅を導入経路の一例にする根拠は、比較の主張ではなく設置事例の側にある。**
交通結節点への設置は積み上がっており（エコモ財団の冊子の 7 事例はすべて
空港とフェリーターミナル）、**鉄道駅はまだ始まったばかり**（Osaka Metro 夢洲駅・
JR の実証）。**確定の表に入れる事実ほど一次情報に当たること**——
「ホームドア 85.3%」は出所の無い数字で、**全 106 駅で整備完了**が正しかった。

**動画の中身は機械で検査できない。** 4・5 枚目は
`tools/shoot_demo.mjs` が公開中のサイトを操作して撮ったもので、
**キャプションの数値が動画の代わりに検査されている**（優先度 2 位の区画の実数）。
**落ちたら数字を書き換えるのではなく、動画を撮り直すこと**——
数字だけ直すと、動画と資料が別の区画を指す。

**台本は速さのほうを基準にする。** 実測 6.5 字/秒は**早口で不自然**だった
（2026-08-17）。**自然なのは 5.5 字/秒**（ニュース読みくらい）で、2 分の枠なら
**600 字前後**が上限になる。**スライドに書いてあることを読み上げない**——
タイトルと大きな数字は見れば分かるので、口では画面に書いていないことだけを言う。
**動画が見せているものを言葉でなぞらない**（同じことを言うと二重になる）。
**接続詞で一本に繋ぐ**——枚ごとに独立した説明を並べると、聞く側が
毎回「いまどこの話か」を組み立て直すことになる。
**動画の尺（17 / 16 / 9 秒）はその区間の下限**で、沈黙は事故ではない。

**スライドは 11 枚。「そのスライドを 2 秒見て何が伝わるか」で作る。**
表紙 → 概要 → どう探すか → 東京全体を比較 → 使用例 → 重みを変えて比較 →
分析から見えてきた場所 → 社会実装の一例：都営地下鉄 → 今後の課題と展望 →
技術スタック → 参考文献（2026-08-17・ユーザー判断）。
**「実際に、こう使う」と書かない**——推奨手順に読める。この道具は決まった順で
使うものではないので**「使用例」**にする。**外部照合（既存 11 か所との比較）は
本編に置かない**——面白い検証だが、初見の人には「それで何が分かったの？」になる。
**技術スタックの枚に使っていないものを書かない**（React も ODPT も使っていない）。

**提出するスライドは PPTX（動画が再生される）。** PDF も併せて出す。
**PPTX でフォントが崩れないのは、文字を 1 つも入れていないから**である
——各ページは `slides.html` を焼いた画像 1 枚で、動画だけをその上に置く
（`tools/slides_pptx.mjs`）。**動画の位置は HTML の `[data-video]` が唯一の出所。**

**「既存施設では到達不可」は失敗ではなく提言。** ホスト施設が 700m 以内に無い
メッシュのこと。現在 1,058/9,507 件（11.1%）。ただし **23 区すべてで公共施設一覧を
投入した結果、この率はデータの有無ではなく非市街地の面積比でほぼ決まる**ように
なった。皇居・羽田空港・埋立地・河川敷を含む区で高く出る。
**区をまたいで並べるなら「区内で優先度が中位以上の到達不可区画」を使う**
（全体 148 件。`docs/issues.md` A9）。この指標は `--report` が出す
（`etl/hosts.py` の `reach_report`。しきい値は区内の優先度の中央値で、
母数は区内の全メッシュ。**手計算しないこと**——かつて手で出した 127 件が
優先度の変わったあとも文書に残り、古くなったことに気付けなかった）。

**画面の見出しに置くのは 148 のほうで、1,058 ではない**（2026-08-04 に
入れ替え。入れ替えた時点では 151 件だった）。1,058 の順位の中央値は
**9,507 件中 8,083 位**で、
**上位 100 区画には 0 件**しか入らない。非市街地が主成分だという性質は
`issues.md` に書いてあったのに、**画面での扱いだけが「いちばん大きい発見」の
ままだった**。**格下げであって撤去ではない。**
**「どこなのか」は地図に重ねる層で出す**（斜線・既定は非表示。
**重ねるのは 148 のほうで、1,058 ではない**——見出しの数と地図の区画が
7 倍ずれていた）。数字だけ大きく出して場所を見せない状態が長く続いた。

**ただし 148 は「重みに依存しない」ではない**（2026-08-06 に訂正）。
重みを通らないのは**到達不可の判定そのもの（1,058）だけ**で、
そこへ重ねている「区内で優先度が中位以上」は優先度＝重みで動く。
実測でプリセットにより **133〜171 件**、既定との共通は **107〜139 件**
（±30% を 200 回で 119〜187 件）。画面が「スライダーを動かしても
変わりません」と書けていたのは、**配信時に凍らせた数を出していたから**である。
いまはブラウザが `ui.midOrAboveSet` で数え直す（規則は `hosts.reach_report` と
同じ。既定の重みで配信値と一致することを起動時に突き合わせる）。
**「重みに依存しない」と「価値判断に依存しない」も違う**——148 は
需要の定義・供給の定義・700m・「区内で中位以上」という 4 つの設計判断の上に
立っており、言えるのは**「この方法の内側での結果」まで**である。

**リンクが開けることは、そこにデータがある証拠にならない。** 列名の決め打ちと
同じ壊れ方を出典欄でもやった（2026-08-04・指摘されるまで気付かず）。画面に
出していた 12 本のうち **4 本が誤り**——2 本は 404、**2 本は 200 が返るのに
使った年度版がそのページに無い**（N03 は 2024 年版を使いながら最新が
2023 年版のページを、P29 は 2023 年版を使いながら 2013 年版しか無い
ページを指していた）。**後ろ 2 件は押しても気付けない。**
さらに騒音は**ラベルごと別のデータセットの名前**だった
（「自動車騒音 要請限度測定結果」は目黒区のもの。読んでいるのは
環境局の「自動車交通騒音調査結果」）。

対処は列名と同じで**中身で裏を取る**。`Source.file_hint` に実際に
読み込んだファイル名を書き、`tools/link_check.py` が
「リンク先にその名前があること」まで検査する。**複数書ける**——
騒音は 1 ページから 5 年度分を取るので、5 つとも載っていることを求める
（1 年度だけ照合すると、ページが改組されて古い年度が落ちても通る）。
ファイル名が URL に出ない出典（環境局のリンクは `/documents/d/kankyo/-_-27`）は
年度の見出しで照合する。都のカタログは HTML が
bot 対策の裏にいるので **CKAN の API 側**で確かめる（API は素通しで、
404 を JSON で正しく返す）。**チャレンジページを「200 だから通す」に
してはいけない**——それをやると「リンク切れでも通る」検査に戻る。

**廃止した概念は、画面から消えたか必ず確かめる。** 2026-08-03 に「地区」を
やめたのに、凡例と限界の項目の **2 箇所だけ文言が残り**、
**画面にしか存在しない概念**を 1 日主張していた（外部から
「地区って結局なんなの？」と聞かれて発覚）。コードの側は
`cluster` という中立な名前だったので、grep しても*歴史として書いた説明*と
*消し忘れ*の区別が付かない。**現在形で書かれた文言だけを見ること。**

## 壊れ方の型 — 列名の決め打ち

一度やらかしている。仕様書から推測した A29 の列名が外れ、**全件 NaN のまま
既定値が全メッシュに乗り、ビルドもテストも成功と表示された**。負荷スコアの
主軸が消えているのに出力を見ても気付けなかった。

そのため、数値に影響する列は `require_column` を通す。

1. 列名で見つかっても、**中身が合わなければ採らない**（`resolve_column`）
2. 列名で見つからなければ列の**中身**から探す（`detect_column`）。何を選んだか表示する
3. それでも特定できなければ、**各列の実際の値を添えて例外で止まる**
4. 絞り込みの結果が 0 件でも止まる（`require_nonempty`）

**同じ列名が版によって別のものを指す。** P29 学校は第1.1版と第2.0版で列の意味が
1 つずれており、`P29_004` は前者で学校分類コード・後者で名称。`P29_009` は
第2.0版でキャンパス名だが、児童生徒数だと思って数値化すると全件 NaN のまま
既定値 150 人が乗る。**列名が実在することは、それが目的の列である証拠にならない。**

新しいレイヤーの正規化を書くときも同じ形にする。`if col:` で分岐して既定値を
入れる書き方をしない。表示にしか使わない列は `optional_column`（警告は出す）。

**「ただの正の整数」からは意味を当てない。** 児童生徒数と建築年は値域が重なる。
列名の裏付けが無ければ推定しない（`require_hint=True`）。実際、施設名称の推定が
`P14_001`（"東京都"）を掴む誤りを作り込んで検査で捕まえている。

**ファイル名からも意味を当てない。** WAM NET の `sfkopendata_202603_13.zip` の
13 は都道府県コードではなくサービス種別（行動援護）。東京都のつもりで開くと
全国の訪問系事業所だけが需要レイヤーに載る。

**名称からも意味を当てない。** 区の公共施設一覧には駐輪場・公衆便所・指定喫煙場所が
施設と同じ粒度で載っている。名称の「駅」でホスト種別を判定していたため
渋谷区 32 件・世田谷区 10 件が供給側に混ざっていた（`HOST_EXCLUDE_PATTERNS` で落とす）。
**供給側が過大になる誤りは「到達できている」と見えるだけで、出力を眺めても気付けない。**

**フォルダ名からも意味を当てない。** 「北区」として渡されたフォルダの中身が
大田区だった（ファイル名の `131113` は大田区の団体コード、`otaku` は「大田区」）。
気付かずに入れると **北区は過大・大田区は過小**で両方の到達不可が同時に狂う。

**逆に、別の手掛かりで裏を取れるなら取る。** P29 の学校分類コード列は、
名称に「特別支援学校／養護学校／盲学校／聾学校」を含む行（都内 48 件）を
最も多く拾える列として選んでいる。取りこぼし（名称で分かる行が漏れる）と
混入（絞った結果の半分が特別支援学校らしくない）の両方で止まる。

江東区の公共施設一覧は**名称の列名が「列1」**（表計算の既定値に化けている）。
一致率では「小区分」「担当課」と区別できなかったが、**列全体の一意率**
（1.00 対 0.06 / 0.04）と、**文字数が「名称_カナ」と r=+0.882 で相関する**ことで
裏が取れた。位置（4 列目）では当てていない。**どちらも中身から出る指標である。**

**欠測が意味を持ってしまう経路もある。** `normalize_ward` が
`str(value or "")` と書かれていたため、`float('nan')` が真であることから
`"nan"` が区名として持ち回られ、**公開中の提言 10 件のうち 3 件に
「ただしnanの施設であり、対象区の所管外」**と出ていた（千代田区の施設なのに逆）。
`selftest` に `normalize_ward(float("nan")) == ""` を入れてある。

**略称と正式名称は混在する。** P04 の診療科目欄は「歯科」と「歯」、
「精神科」と「精」が同じ列に入る。正式名称の部分一致だけで絞ると
約 1 割を静かに落とす。略称は 1〜2 文字なので部分一致にはできない
（「神」は神経内科「神内」の一部）。区切って完全一致で判定する。

逃げ道は `--assume-missing`（P29 の在籍者数・WAM NET の定員のみ）。既定では開かない。
**P29 に在籍者数が無いのは異常ではなく仕様**なので、既定は「止まる」ではなく
「`--students` で外から当てる」。`--assume-missing` はその出典すら無いときの逃げ道で、
使うとこの層は「近くに特別支援学校があるか」というフラグに戻る。

## ひとつのレイヤーを複数の出典が書く

`hosts` ← `p14-hosts`（児童館）と `facilities`（区の公共施設一覧）
`welfare` ← `p14` と `wamnet`（現在は `wamnet` で置き換え済み。併置すると
同じ事業所を別名で二重に数える）
`schools` ← `p29`（**位置**）と `--students`（**規模**）。後者は地物ではなく
属性を当てる表なので、並べて渡す位置引数ではなく別の引数にしてある
（同じ列に混ぜると「どちらが位置の出典か」が消える）

分割されたファイルは並べて渡す。WAM NET はサービス種別ごとに 29 分割で、
都内に十数件しかない種別は対象区に 1 件も無いことがある——**複数ファイルを渡した
ときだけ**「このファイルは 0 件」を警告で通し、総数 0 なら止める。

**供給側は 23 区で 30 本近いファイルを並べる。** 区ごとに様式が違い、
統合一覧を持たない区（港・江戸川・中野）、KML の区（足立）、xlsx の区（北・大田）が
ある。**毎回すべてを並べて渡す**（同じ出典ラベルなので 1 本だけ渡すと他区が消える）。
実際に使うコマンドは [`docs/handoff-hosts.md`](docs/handoff-hosts.md) にある。

既存の出典が消える場合は書かずに止まる。`--append`（統合、同名かつ 100m 以内は
1 件に寄せる。**名寄せは区名の接頭辞と「（複合施設）」を外してから比較する**——
「北新宿図書館」と「新宿区立北新宿図書館」が寄らず 63 組が二重に入っていた）か
`--replace`（入れ替え）を明示する。
`--append` で同じ出典を入れ直したときは、既存の古い行を捨てて新しい行を採る
（逆にしていたため、正規化を直して再実行しても出力が 1 バイトも変わらなかった）。

区ごとに分かれたファイルは並べて渡せる:

```bash
python -m etl.fetch --inspect   ~/Downloads/<フォルダかファイル>
python -m etl.fetch --normalize facilities ~/Downloads/渋谷区.csv ~/Downloads/世田谷区.csv --append
python -m etl.build --live
```

`--normalize` にフォルダを渡せるのは `a29` と `s12` だけ。CSV のレイヤー
（`noise` `facilities` `estat-pop`）は CSV 専用（Excel なら先に書き出す）。

**同じデータが文字コード違いで 2 部入っていることがある。** S12 の ZIP は
`UTF-8/` と `Shift-JIS/` に同一データを持つ。フォルダごと読むと全駅の乗降客数が
ちょうど 2 倍になる。件数も列構成も同じなので結合後を見ても気付けない
（`read_vector` がファイル名で寄せて 1 つだけ読む）。

**1 行が 1 施設とは限らない。** S12 は「駅×事業者×路線」で 1 行。渋谷駅は 4 行あり、
1 行だけ採ると 3 分の 1 を落とす。グループコードで束ねて合算する。
都教委の在籍者数 CSV も「学校×障害種別」で 1 行で、併置校 17 校が複数行に分かれる
（光明学園 = 肢体 209 + 病弱 47）。**取りこぼす方向なので出力を見ても気付けない**ため、
学校番号で束ねたあと**ファイルの合計行（15,258 人）と突き合わせて検算する**。

**公立だけの調査で全件を賄おうとしない。** 都教委の在籍者数調査は公立が対象で、
23 区 45 校のうち国立 3・私立 3 が載らない。ここを「一致しなかった」として
既定値に流すと、**名寄せの綻びと調査対象外が同じ見た目になる**。
P29 の設置者コード（`P29_006`）で切り分け、公立が 1 校でも漏れたら止める。
**設置者コードの列も列名では決めない**——名称から作った 3 グループ
（区市町村立・大学附属・都立）が 3 つの別々の値へきれいに割れることを条件にする。

## 数字を出す前に確認すること

現在 **実データ 10/10 レイヤー**（模擬なし）。警告バナーも出ない。
だが **模擬が消えたことと数値が確からしいことは別**で、出典自体の弱さが残る。

- **騒音は幹線道路の道路端しか測っていない。** 23 区の内側にある測定点は
  965 点（区ごとに 17〜65 点）で、**測定点の選ばれ方はまだ区に依存する**。
  「世田谷ダミーとの相関」は 2 区時点 +0.448 → 要請限度 383 点 +0.234 →
  常時監視 5 年分 700 点 +0.133 と下がってきたが、**要請限度を入れ直して
  +0.249 へ戻した**（2026-08-05・ユーザー判断）。0 ではないどころか、
  **世田谷区の内挿平均 68.7dB は 23 区で最も高い**。
  区ごとの内挿平均も 64.7dB（練馬区）〜68.7dB（世田谷区）と 4.0dB 開く。
  **手計算しないこと**——`--report` が出す（`score.ward_dependence_report`・
  `ward_value_spread`。`meta.ward_dependence` として配信し `doc_numbers` が検査）。
  **区は決め打たない側も見ること**——騒音の区依存が最大なのは
  **世田谷区ではなく練馬区（-0.283・符号が逆）**で、A1 が正の側だけを
  名指しで追っていたため 2026-08-06 まで書かれていなかった
  正規化を絶対尺度へ移して増幅はやめ、出典の入れ替えで偏りも軽くなったが、
  静かなのではなく測っていない場所があることは変わらない
- **騒音の欠測を中央値で埋めている。** 観測圏外 458 セル（`issues.md` A6・未解決。
  測定点が 383 → 1,058 点になって 1,095 セルから減った）
- **騒音は 2 つの調査の混合。** 常時監視（幹線道路を系統的にローテーション）と
  要請限度（**苦情が出た道路**）。後者は **文京区・渋谷区で 5 年分でも 0 点**で、
  区への依存がそのまま残っている。どちらの調査の点かは `survey` 列で画面まで運ぶ
- **混雑は昼間人口ではなく従業者数。** 昼間人口のメッシュ統計が配信されていないため、
  経済センサスの従業者数で代替。買い物客・通学者を含まない
- **特別支援学校の規模は 45 校中 42 校が実数**（計 9,926 人・**22〜654 人**と 29.7 倍）。
  P29 は在籍者数を持たないので都教委の在籍者数 CSV から当てている。
  国立 3 校は在籍者数を公表しておらず既定値 150 人のまま（`students_assumed`）。
  **規模を入れても Leave-one-out は 0% のまま**で、依存は解消していない（A4）
- 供給側は 23 区すべての公共施設一覧を入れた（ホスト 1,531 件）が、
  **都・国・民間の施設は入っていない**。また **北区だけは中身が薄い**
  （22 件のうち 20 件が P14 の児童館で、図書館が 1 件も無い。区が一覧を公開していない）
- **ホスト件数を「施設の数」として読めない。** 100m 以内に別の行が並ぶ組が **459 組**ある。
  表記違いの 63 組は是正したが、「中町児童館」と「中町図書館」のような
  **別種別 381 組**が残る（到達可否は動かない。`issues.md` の独立項目）。
  **手計算しないこと**——`--report` が出す（`etl/hosts.py` の `proximity_report`）
- **予約制の無人貸室を供給側に数えている**（集会所 103 件。ユーザーの判断）。
  図書館・出張所と同じホストとして地図に出る（`issues.md` の独立項目）
- 需要側の定員が実測なのは **31.0%**（4,370 / 14,118）。訪問系・相談系・
  共同生活援助には制度上
  定員が無く、種別ごとの仮定員で補っている（欠測ではない）。
  定員欄が空だったのは 9,748 行（69.0%）で、**需要寄与では 25.1%**。
  **この行を全部落としても上位 10 件は 90% 残る**（`issues.md` A3）
- **重み以外の固定値 76 個を同時に ±30% 揺さぶると上位 10 件の重なりは 52.3%**
  （最悪 0%）。群ごとでは仮定員 99%・**騒音の内挿 93%**・種別重み 93% と軽く、
  **帯域が 71%・最悪 10% で最も重い**（用途地域の負荷値 72.5% がこれに次ぐが、
  最悪値は 0%）。帯域の重さは丸ごと
  **特別支援学校 1 層**から来ている（単独 ±30% で 73.3%）
- **重み 8 個を ±30% 揺さぶると上位 10 件の重なりは 72.1%**（最悪 0%）。
  **4 プリセット全てで上位 20 件に入る区画は 0 件**——
  「重みの選び方に関係なく上位に来る場所」はもう無い（`issues.md` A4）
- 年次がそろっていない（事業所 2026・駅 2024・騒音 2019〜2024・混雑 2021・
  用途地域 2019・公園 2011）
- 順位は対象地域内の相対値。色は絶対的な危険度ではない
- 当事者による検証を経ていない

**上位は渋谷駅前ではない。** 提言は**区画単位**の順位表で、既定は上位 50 件
（画面で 20 / 50 / 100 に切り替えられる）。上位 10 区画の区内訳は
**文京 3・千代田 3・板橋 2・江東 1・豊島 1**で、
1 位は江東区 亀戸（`5339463631`・優先度 0.974）、以下 水道橋 ×3・板橋…と続く。
**渋谷駅前は 802 位 / 9,507 件**（メッシュ `5339358633`・優先度 0.7218）。
負荷は最大でも需要側の事業所が中位のため。
**3,469 位と書いてあったら誤り**——検査が渋谷駅の 700m 南（代官山）の
メッシュを見ていた。`tools/doc_numbers.py` が中身で裏を取るようにしてある。

**「地区」はもう作らない**（2026-08-03）。かつては上位 40 区画を格子隣接で
束ね、「豊島区 大塚・北池袋周辺（6 区画）」のような地区を提言の単位にしていた。
束ね方（格子隣接）には根拠があったが、**どこまでを束ねるかには無かった**——
地区の広がりは母数 40 で決まり、40 のあたりに切れ目は無い
（40 位 0.9510 / 60 位 0.9435 / 100 位 0.9272 と連続する）。
つまり「6 区画」の 6 は場所の性質ではなく 40 で切ったことの帰結で、
**「○○周辺」という名前が、計算していない広がりを主張していた**。
8 近傍か 4 近傍かにも根拠が無い（3 位の区画は 8 近傍で 6・4 近傍で 3）。
**隣接は単位ではなく記述として残し、必ず母数を添える**
（「上位 50 区画のうち 3 区画が接している」）。

**表示件数は `RANKING_OPTIONS`（20/50/100）と `RANKING_DEFAULT_N`（50）で、
`config.py` が唯一の出所。** かつて `proposals.json` は根拠カード（上位 20 区画）から、
画面は上位 40 区画から束ねており、**同じ「提言リスト」が別物だった**
（資料に添付した JSON に無い地区が画面に出る）。いまは両方とも
`build_cards` の出力から作るので、別物になり得ない。
**件数を選べるようにしてあるのは、打ち切りに根拠が無いことを隠さないため。**

**過去の数字を引用しないこと。** 対象区とデータが変わるたびに上位は入れ替わっている:
模擬データの頃の「上位 10 件がすべて渋谷駅 583m 以内」、A1 是正前の
「9 件が渋谷区北部・1 件が三軒茶屋・渋谷駅前 85 位」、2 区時点の
「上位 10 件がすべて渋谷区北部・渋谷駅前 79 位」、A10 是正前の
「上位 10 件は豊島区 7・千代田区 2・文京区 1」（**施設単位で数えていた頃の件数**）、
A4 是正前（全校 150 人扱い）の「新宿区ほか 都庁前・南新宿周辺 / 千代田区ほか
神保町・水道橋周辺 18 区画・渋谷駅前 519 位」、**特別支援学校の帯域が 1,200m
だった頃の「豊島区 池袋・大塚周辺 17 区画 / 大田区 蒲田・蓮沼周辺 /
渋谷駅前 642 位」**、**騒音が平成25年度 383 点だった頃の
「上位 10 件は文京 4・豊島 3・千代田 2・江東 1 / 1 位 豊島区 大塚 0.990 /
渋谷駅前 792 位」**、**騒音が常時監視 5 年分 768 点だけだった頃の
「上位 10 件は文京 3・板橋 2・千代田 2・江東 1・墨田 1・豊島 1 /
渋谷駅前 798 位・0.7226」**——**いずれも現在の出力ではない。**
**特別支援学校の規模を実数にしただけで提言 8 件のうち 4 件が入れ替わり、
その帯域を 800m へ直しただけで提言が 8 件 → 11 件になり顔ぶれも変わった。
この 1 層で毎回結論が動く**（`issues.md` A4）。
**騒音の出典を常時監視 5 年分へ入れ替えたときも上位 10 件の重なりは 6/10 で、
旧 1 位（豊島区 大塚）は 28 位へ落ちた**（2026-08-04）。
**そこへ要請限度を戻したときは 9/10・令和6年度を足したときは上位 5 件が不変**で、
入れ替えほど大きくは動いていない（2026-08-05）。
**なお「提言 N 件」という数え方自体、地区単位だった頃のもの**——
2026-08-03 に単位を区画へ戻したので、いまの提言は順位表そのものである。

## 生成物の扱い

`web/public/data/` は ETL の生成物だが追跡対象（クローンしただけで地図が
開けることを優先）。ビルド後は差分が出るので `git pull` が止まる。捨ててよい。

```bash
git checkout -- web/public/data/
```

**生成物は環境依存で 4 桁目がズレる**（用途地域の面積加重平均が GEOS の
バージョン差で変わる）。上位 50 件の順位は不変であることを確認済み。
プレゼンで出す数値は 1 つの環境で生成したものに揃えること。

## 書き方

コメントと文書は日本語。**なぜそうしたか**を書く。何をしているかはコードが示す。
不利な結果（感度分析で駅レイヤーへの依存が大きいこと、需要の過小計上など）も
隠さず書く — それがこの作品の中身でもある。
